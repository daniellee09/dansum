---
// 구글 OAuth 하나만 쓰면서 가입과 로그인의 구분이 없어졌다(처음이면 자동 가입).
// 기존 /register 링크(헤더·댓글 안내 등)가 깨지지 않도록 라우트는 남기고 로그인으로 넘긴다.
// redirect 같은 파라미터는 그대로 이어준다.
const params = Astro.url.searchParams.toString();
return Astro.redirect(`/login${params ? `?${params}` : ""}`, 302);
---
